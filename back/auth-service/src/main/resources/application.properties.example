spring.application.name=auth-service
server.port=8081

# JWT
jwt.secret=${JWT_SECRET:change-me}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Downstream services
services.user.base-url=${SERVICES_USER_BASE_URL:http://localhost:8082}
